// Stub module for Nuxt's optional `experimental.appManifest`.
// Nuxt's runtime code contains a dynamic `import("#app-manifest")` on the server side.
// Even when the feature is disabled, Vite may still try to resolve the specifier during dev.
export default {};
